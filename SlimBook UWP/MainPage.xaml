<Page
    x:Class="SlimBook_UWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SlimBook_UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Resources>
        <Storyboard x:Name="iconRotation" AutoReverse="False" RepeatBehavior="Forever">
            <DoubleAnimation Duration="0:0:1" To="360" Storyboard.TargetProperty="(UIElement.RenderTransform).(CompositeTransform.Rotation)" Storyboard.TargetName="refreshIcon" />
        </Storyboard>
    </Page.Resources>
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        
        <CommandBar VerticalAlignment="Bottom" Background="#FF3B5998" Foreground="White" Canvas.ZIndex="1">
            <CommandBar.SecondaryCommands>
                <AppBarButton x:Name="abbAbout" Label="About" Foreground="White" Background="#FF3B5998" />
                <AppBarButton x:Name="abbOpenSource" Label="Github" Foreground="White" Background="#FF3B5998" />
            </CommandBar.SecondaryCommands>
            <CommandBar.Content>
                <Grid />
            </CommandBar.Content>
            <AppBarButton x:Name="abbBack" Icon="Back" Label="Back" Foreground="White" />
            <AppBarButton x:Name="abbUp" Icon="Up" Label="Top" Foreground="White" />
            <AppBarButton x:Uid="refreshButton" x:Name="abbRefresh" RenderTransformOrigin="0.5,0.5" Foreground="White" Label="Refresh" >
                <AppBarButton.Icon>
                    <SymbolIcon x:Name="refreshIcon"  Symbol="Refresh" RenderTransformOrigin="0.5,0.5" >
                        <SymbolIcon.RenderTransform>
                            <CompositeTransform/>
                        </SymbolIcon.RenderTransform>
                    </SymbolIcon>
                </AppBarButton.Icon>
            </AppBarButton>
            <AppBarButton x:Name="abbHome" Icon="Home" Label="Home" Foreground="White" />
            <AppBarButton x:Name="abbFullscreen" Icon="FullScreen" Label="Full Screen" Foreground="White" Visibility="Collapsed" />
        </CommandBar>
        <Grid x:Name="_Setting" Background="Black" Canvas.ZIndex="1" Visibility="Collapsed" Margin="10,10,10,10">
            <Grid x:Name="_SETTINGS_B" VerticalAlignment="Top" Height="50" BorderThickness="0" BorderBrush="Black">
                <TextBlock x:Name="txtSETTINGSTitle" Margin="10,0" TextWrapping="Wrap" Text="TEXT" VerticalAlignment="Center" FontSize="24" Height="Auto" Foreground="White" />
                <Button x:Name="CloseGrid" Content="&#xE711;" HorizontalAlignment="Right" VerticalAlignment="Center" FontFamily="Segoe MDL2 Assets" Margin="0,0,10,0" FontSize="24" Foreground="White" Background="#00000000" Canvas.ZIndex="2" />
            </Grid>
            <Pivot x:Name="PivotSettingsAbout" Margin="0,50,0,0" Foreground="White" Background="Black">
                <PivotItem x:Name="_PivotAbout" Header="About" Foreground="White">
                    <StackPanel>
                        <Image Height="100" HorizontalAlignment="Left" Source="Assets/Square44x44Logo.targetsize-256.png" />
                        <TextBlock x:Name="AppName" FontSize="20" TextWrapping="Wrap" Text="SlimBook UWP" />
                        <TextBlock  Name="version" Text="version" />
                        <TextBlock x:Name="privacy" TextWrapping="Wrap" Text="privacy" />
                    </StackPanel>
                </PivotItem>
                <PivotItem x:Name="_PivotCredits" Header="Credits" Foreground="White">
                    <StackPanel>
                        <TextBlock x:Name="developers" Text="Developers" FontSize="24" />
                        <HyperlinkButton x:Name="hyperDev" Content="Celestial Doom" HorizontalAlignment="Left" VerticalAlignment="Stretch" FontSize="22" />
                        <Grid Height="50" />
                        <Image Height="100" HorizontalAlignment="Left" Source="Assets/Square44x44Logo.targetsize-256.png" />
                        <TextBlock Text="App Logo" TextWrapping="Wrap" FontSize="24" />
                        <HyperlinkButton x:Name="hyperLogo" Content="Icon Archive" HorizontalAlignment="Left" VerticalAlignment="Stretch" FontSize="22" />
                        <TextBlock Text="Donations" TextWrapping="Wrap" FontSize="24" Visibility="Collapsed" />
                        <Button Content="Donations" HorizontalAlignment="Left" VerticalAlignment="Stretch" FontSize="22" Foreground="White" Visibility="Collapsed" />
                    </StackPanel>
                </PivotItem>
                <PivotItem x:Name="PivotDevNotes" Header="Dev Notes">
                    <ScrollViewer x:Name="myScrollView" IsTabStop="True" VerticalScrollBarVisibility="Auto" Margin="1,1,1,1" BorderBrush="#FFA0A0A0">
                        <TextBlock x:Name="DevText" Margin="0" TextWrapping="Wrap" Text="TEXT" VerticalAlignment="Top" FontSize="24" Foreground="White" />
                    </ScrollViewer>
                </PivotItem>
            </Pivot>
        </Grid>
        <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <ProgressRing x:Name="ProgRing" HorizontalAlignment="Center" VerticalAlignment="Center" Width="60" Height="60" Foreground="#FF3B5998" Canvas.ZIndex="1" IsActive="True" Visibility="Collapsed" />
            <WebView x:Name="SlimBookUWPWebView"  Source="https://touch.facebook.com/home.php" Margin="0,0,0,48" />
        </Grid>
    </Grid>
</Page>